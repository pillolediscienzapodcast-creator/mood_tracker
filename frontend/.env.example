# Base URL delle API del backend.
# In sviluppo lasciare "/api/v1": le richieste passano dal proxy di Vite
# (vedi vite.config.ts) che le inoltra a http://localhost:8000.
# In produzione impostare l'URL completo, es. https://api.example.com/api/v1
VITE_API_BASE_URL=/api/v1
